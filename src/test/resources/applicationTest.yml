spring:
  application:
    name: g2_testing
  datasource:
    url: jdbc:h2:mem:tests_db
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: false
  h2:
    console:
      enabled: true
  profile:
    active: true